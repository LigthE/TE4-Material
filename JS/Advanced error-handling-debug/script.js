console.log("Hello, World!");

/*test
const divide = (a, b) => {
  if (b === 0) throw new error("numbers cannot be divided by 0");
  console.log(a / b);
};

divide(20, 8);
*/

//A

const multi = (a, b) => {
    try{
  if (a !== "number" || b !== "number") throw new error("numbers cannot be multiplied by 0");
  console.log(a * b);
}catch (error)

multi(20, 5);
}


//B

//C
